# 工程骨架的架构边界

本文件只说明任务 01 已建立的边界，不代表后续业务已经实现。

## 依赖方向

```text
界面层 → 用例层 → 领域模型层 ← 存储层
                     ↑
            规则解析层、备份编解码层
```

- 界面层只调用用例层，不直接读写存储。
- 用例层通过领域层声明的 `ManagementSnapshotStore` 协议取得数据。
- 存储层通过 `SwiftDataProjectRepository` 提供本地持久化入口，并由应用启动时创建的 `ModelContainer` 注入管理用例；项目管理界面仍属于后续任务范围。
- 规则解析层当前只执行本地、无副作用的输入有效性判断；正式规则解析属于后续任务范围。
- 备份编解码层提供独立的 JSON 编码与解码边界；备份文件格式和导入导出流程属于后续任务范围。

## 交互与权限边界

- `MenuBarExtra` 仅提供菜单栏入口，并通过 `openWindow` 打开独立管理窗口。
- 管理窗口用于低频管理信息展示，不承载菜单栏常用操作。
- 本工程没有 `NSPasteboard` 调用、没有剪贴板监听，也没有网络调用。后续若需要粘贴读取，只能由用户主动点击后触发。
- 工程不引入第三方依赖，不配置 CI/CD；构建、分析、测试与发布均应在工具链就绪后人工执行。

## 迁移策略

无迁移，直接替换。当前仓库没有既有应用数据或旧工程需要导入。
